<div class="pt-4 text-center">
	<% if @event.admin != current_user %>
<%= form_tag event_participations_path do %>
  <article>
    <% if flash[:error].present? %>
      <div id="error_explanation">
        <p><%= flash[:error] %></p>
      </div>
    <% end %>
    <label class="amount">
      <span>Amount: <%= @event.price %> €</span>
    </label>
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="A month's subscription"
          data-amount="<%= @event.price %> €"
          data-locale="auto"></script>
<% end %>
<% else %>
 <strong class="card text-center mt-3 mr-3 ml-4 pt-2" ><p>Eeeeee le con tu doit pas paye parceque tu es l'organisateur de cette evenement</p></strong>
<% end %>
</div>
